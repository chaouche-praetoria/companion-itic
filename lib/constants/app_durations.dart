class AppDurations {
  static const Duration splashGradient = Duration(seconds: 3);
  static const Duration splashText = Duration(milliseconds: 1500);
  static const Duration splashTimer = Duration(seconds: 3);
}
